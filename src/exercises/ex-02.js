const myname = process. argv [2];
const myage = Number (process. argv [3]); // Man muss Nuber schreiben, damit die Rechnung ausgeführt wird, sonst würde nur 164 stehen.

console.log(`Hallo ${myname} dein alter ist ${myage + 4}.`);